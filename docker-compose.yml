version: '3.8'

services:
  visualizer:
    build: .
    container_name: data-structures-visualizer
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ./data:/app/data
    working_dir: /app
